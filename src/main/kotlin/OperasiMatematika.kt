fun  main(){
    var x: Int = 20
    var y: Int = 10

    var handlePlus: Int = x + y
    var handleMin: Int = x - y
    var handleMultiply: Int = x * y
    var handleDivide: Int = x / y
    var handleMod: Int = x % y

    println(handlePlus)
    println(handleMin)
    println(handleMultiply)
    println(handleDivide)
    println(handleMod)
}